ext {
    // AdMob配置
    admob = [applicationId: "ca-app-pub-7869168547621070~7888544550", // 测试应用ID，请替换为实际的AdMob应用ID
             adUnitIds    : [banner      : "",        // 横幅广告测试ID
                             interstitial: "ca-app-pub-7869168547621070/2861697163", // 插页广告测试ID
                             splash      : "ca-app-pub-7869168547621070/2211950153",       // 开屏广告测试ID
                             native      : "ca-app-pub-7869168547621070/6776891444",        // 原生广告测试ID
                             full_native : "",        // 全屏原生广告测试ID
                             rewarded    : ""         // 激励广告测试ID
             ]]

    // Google Ad Manager 配置（按业务替换为正式 ID）
    gam = [adUnitIds: [banner      : "",
                       interstitial: "/23312177113/PhoneTrackerLocation_IV",
                       splash      : "/23312177113/PhoneTrackerLocation_SP",
                       native      : "/23312177113/PhoneTrackerLocation_RV",
                       full_native : "",
                       rewarded    : ""
    ]]

    // Pangle配置
    pangle = [applicationId: "8821947", // Pangle测试应用ID
              adUnitIds    : [splash      : "890160424",        // 开屏广告测试ID（竖屏）
                              banner      : "",        // 横幅广告测试ID（320x50）
                              interstitial: "983249806",       // 插页广告测试ID（竖屏）
                              native      : "983250034",        // 原生广告测试ID
                              full_native : "",        // 全屏原生广告测试ID
                              rewarded    : ""         // 激励视频测试ID（竖屏）
              ]]

    // TopOn配置
    topon = [applicationId: "h1hb7huqefmn3p",           // TopOn 应用 ID（需替换为实际值）
             appKey       : "a978da77b462637eef2fbcbd7edddfb57",          // TopOn 应用密钥（需替换为实际值）
             adUnitIds    : [interstitial: "n1hb7huqje4tup",  // 插页广告位 ID（需替换为实际值）
                             rewarded    : "",  // 激励广告位 ID
                             native      : "n1hb7huqlfpnt5",  // 原生广告位 ID（需替换为实际值）
                             splash      : "n1hb7huqn6knum",  // 开屏广告位 ID（需替换为实际值）
                             full_native : "",  // 全屏原生广告位 ID（需替换为实际值）
                             banner      : ""]  // 横幅广告位 ID（需替换为实际值）
    ]

    // MAX (AppLovin) 配置
    max = [sdkKey   : "",  // Demo SDK Key（测试用）
           adUnitIds: [splash      : "",      // 开屏广告位 ID
                       banner      : "",      // 横幅广告位 ID
                       interstitial: "",// 插页广告位 ID
                       native      : "",      // 原生广告位 ID
                       fullNative  : "", // 全屏原生广告位 ID
                       rewarded    : ""]    // 激励广告位 ID
    ]

    // 应用版本配置
    app = [applicationId: "com.phonetracker.sharing.tool",
           compileSdk   : 36,
           minSdk       : 26,
           targetSdk    : 36,
           versionCode  : 1,
           versionName  : "1.0.0"]

    // 统计归因配置
    analytics = [adjustAppToken: "",           // Adjust App Token
                 thinkingDataAppId: "",   // 数数 SDK APP ID
                 thinkingDataServerUrl: "",     // 数数上报域名
                 defaultUserChannel: "natural"]                           // 默认用户渠道，internal默认paid
}
